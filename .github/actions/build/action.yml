name: GitHub Actions Demo - File 2

on:
  push:
    branches:
      - master

jobs:
  The-action-that-should-run-afterwards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Cool Step
        run: echo "Cool step from file 2."
